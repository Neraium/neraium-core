# GROW DEMO

Default demo command:

```bash
python run_demo.py
```

The built-in demo includes:

- stable room behavior
- early hidden drift before threshold breach
- subsystem-driven room instability
- concentrated zone-level biological risk
- plant-proxy divergence
- facility comparison
- multi-site portfolio comparison

Use the scope selector in the default UI to zoom from plant to portfolio.
