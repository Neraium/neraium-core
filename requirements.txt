# Root requirements for platform deploys that install from /requirements.txt.
# Keep this file explicit instead of only using a nested -r include.
fastapi>=0.110
uvicorn>=0.29
pydantic>=2
numpy>=1.24
scikit-learn>=1.3

# Additional packages used by the API app.
pandas>=2.0
python-multipart>=0.0.9
starlette>=0.36
