version: 1.0
runtime: python311

build:
  commands:
    build:
      - pip install --upgrade pip
      - pip install -r requirements.txt

run:
  runtime-version: 3.11
  command: sh -c "uvicorn neraium_core.markets.app.markets_api:app --host 0.0.0.0 --port ${PORT:-8000}"
  network:
    port: 8000